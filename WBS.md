# WBS.md — 사무실 맛집 지도 (hannuri-matzip)

`SPEC.md`(설계) + `SHELL.md`(화면 구조) 기준 작업 분해.
각 태스크는 독립 커밋 단위이며,
**완료 조건(DoD)을 만족하지 못하면 다음 태스크로 넘어가지 않는다.**

진행 표기: `[ ]` 대기 / `[~]` 진행중 / `[x]` 완료

---

## P0 — 기반 (반나절)

### [x] 0.1 프로젝트 스캐폴딩
- `create-next-app` — TypeScript, Tailwind, App Router, `src/` 디렉터리
- ESLint + Prettier, `strict: true`
- 디렉터리 구조 생성:
  ```
  src/
    app/
      @modal/       # 인터셉트 모달 슬롯 (SHELL.md §1)
    components/     # 공용 UI
    features/
      map/          # 지도 컴포넌트 + 훅
      restaurants/
      reviews/
      pick/
    lib/
      supabase/     # client.ts, server.ts
      naver/        # 지역검색 프록시 유틸
    types/
  ```
- `@modal/default.tsx` 포함한 라우트 골격 (없으면 새로고침 시 404)
- **DoD**: `pnpm dev` 실행 시 빈 페이지 렌더, `pnpm build` 통과.
  `src/app/globals.css`는 `@import "tailwindcss"`만 있는 상태 (토큰은 0.5에서)

### [x] 0.2 Supabase 프로젝트 + 스키마
- PostGIS 확장 활성화
- `SPEC.md §2.1` 테이블 전부 마이그레이션 파일로 작성 (`supabase/migrations/`)
- `restaurant_stats` 뷰
- **DoD**: `supabase db reset` 후 스키마 재현 가능,
  테이블 6개 + 뷰 `restaurant_stats` 1개 존재 확인
- ⚠️ 작업 환경에서 Docker 이미지 레지스트리가 차단되어 `supabase db reset` 은 실행하지 못했다.
  `pnpm db:verify`(로컬 Postgres + shim)로 대체 검증했다. 로컬에서 1회 재확인 필요.

### [x] 0.3 RLS 정책
- `SPEC.md §2.3` 정책 전부 적용
- Storage 버킷 `review-photos` 생성 + 정책
- **DoD**: 세션 없는 상태(anon 키만)로 `select * from restaurants` 시 0건/거부,
  익명 세션 발급 후 조회 성공
- ⚠️ `pnpm db:verify` 로 검증. Storage 는 shim 의 근사치라 실물 동작은 미검증이다.

### [x] 0.4 익명 세션
> 로그인 UI 없음. 사용자는 인증이 있다는 사실 자체를 몰라야 한다.

- Supabase 콘솔에서 Anonymous Sign-in 활성화 + rate limit 설정
- 최초 진입 시 세션 없으면 `supabase.auth.signInAnonymously()` 자동 호출
  (레이스 방지: 동시 호출이 겹치지 않도록 단일 프로미스로 감쌀 것)
- `profiles` 자동 생성 트리거 — `display_name` 랜덤 부여 (형용사+명사+4자리)
- 세션 복구 실패 시 조용히 재발급, 사용자에게 에러 노출 금지
- **DoD**
  - 처음 접속한 브라우저에서 로그인 화면 없이 바로 메인이 뜬다
  - 새로고침·재방문 시 같은 `user_id` 유지
  - 시크릿 모드로 열면 다른 `user_id`가 발급된다
  - `profiles`에 닉네임이 자동 생성되어 있다
- `src/lib/supabase/` — `client.ts`(브라우저 싱글턴) / `session.ts`(`ensureSession`)
  / `session-bootstrap.tsx`(layout 에서 1회 마운트, 렌더 없음).
  마이그레이션 `20260808010000_profiles_trigger.sql` — `on_auth_user_created` 트리거,
  `random_display_name()`(형용사 20 × 명사 20 × 4자리 = 400만 조합).
- **단일 프로미스 가드가 실제로 값을 한다.** 동시 5회 호출을 재보면 가드가 있을 때
  `signInAnonymously` 1회, 없을 때 5회다. supabase-js 내부 락은 여기까지 막지 않는다.
  (참고: Next 15 dev 에서 StrictMode 이펙트 이중 실행은 관측되지 않았다. 이 가드가
  막는 건 그게 아니라 P2 에서 조회 훅들이 각자 세션을 기다릴 때 생길 동시 호출이다.)
- 함께 고친 것: `restaurant_stats` 에 `security_invoker = on`. 뷰가 소유자 권한으로
  돌면 세션 없는 요청도 평점·리뷰 수를 읽어서 §2.3 의 "세션 없으면 0건" 이 깨진다.
- `pnpm db:verify` 에 7절 추가. 반례 3종 확인 — 트리거를 빼면 "profiles 가 따라오지
  않았다", `security_invoker` 를 빼면 "세션 없이 restaurant_stats 가 N건 읽혔다",
  단일 프로미스를 빼면 발급이 5회로 늘어난다.
- 브라우저 검증(Playwright, GoTrue 스텁): 최초 발급 / 새로고침·새 탭에 같은 user_id /
  다른 저장소는 다른 user_id / 로그인 UI 0개 / 발급 실패해도 화면 정상 + 에러 비노출 /
  탭 복귀 시 조용히 재시도 — 10개 항목 전부 통과, 페이지 에러 0건.
- 작업 환경 프록시가 `*.supabase.co` 를 403 으로 막아서 여기서는 GoTrue 스텁으로만
  확인했고, **실물은 맥북 Chrome 에서 DoD 4개를 직접 확인했다** — 로그인 화면 없이
  메인 진입 / 새로고침에 같은 `user_id` / 시크릿 창은 다른 `user_id` /
  `profiles` 에 두 세션의 닉네임이 자동 생성(`든든한햄스터8217`, `유쾌한다람쥐2682`).
  `auth.users` 트리거는 `db push` 로 그대로 들어갔다 — storage.objects 와 달리
  소유권 문제가 없다.
- 실물에서 걸린 두 가지. 둘 다 코드가 아니라 콘솔 설정이었다:
  - Supabase 콘솔의 **Allow anonymous sign-ins** 가 꺼져 있으면 `signup` 이 422 다.
    이 토글은 Providers 목록 안이 아니라 그 위 `User Signups` 블록에 있다.
  - NCP 서비스 URL 에 `http://localhost:3000` 이 없으면 지도 auth 가 401 이다.
    배포 도메인만 등록하면 로컬에서만 안 뜬다.
- 422 로 발급이 실패하는 동안에도 화면은 그대로 떴고 에러 문구도 안 나왔다.
  §2.4 의 "조용히 실패" 가 실물에서도 그대로 동작한다.

### [x] 0.5 디자인 토큰 + 기본 컴포넌트
> P1부터 화면을 만들기 시작하므로, **그 전에** 색·타입·간격을 고정한다.
> 건너뛰면 화면마다 색이 미묘하게 달라지고 나중에 전부 되돌려야 한다.

- Pretendard Variable 로드 (`next/font/local` 또는 CDN, `font-display: swap`)
- `design-tokens.css` 내용을 `src/app/globals.css`로 이관
- shadcn/ui 초기화 (`pnpm dlx shadcn@latest init`) — Tailwind v4 모드
  - 추가할 컴포넌트만: `button` `input` `dialog` `sheet` `badge` `skeleton`
    `dropdown-menu` `switch` `toast` `alert-dialog`
  - **전체 추가(`--all`) 금지.** 안 쓰는 컴포넌트가 리뷰 대상이 된다.
- 프로젝트 전용 컴포넌트 3종 (`src/components/`)
  - `<Rating value={4.2} count={12} />` — 채워진 별 + **숫자 병기** (색만으로 정보 전달 금지)
  - `<CategoryChip category="한식" selected />` — 토글, 키보드 조작 가능
  - `<Distance meters={340} />` — 1000m 미만 `340m`, 이상 `1.2km`, `tabular-nums`
- `/design` 라우트에 토큰·컴포넌트 전수 나열 (개발 전용, 배포 시 `noindex`)
- **DoD**
  - `/design`에서 전 토큰과 3종 컴포넌트가 렌더됨
  - `rg -n '#[0-9a-fA-F]{6}' src --glob '!**/globals.css'` → **0건**
  - 360px 폭에서 `/design` 레이아웃 안 깨짐
  - Tab만으로 CategoryChip 토글 가능, 포커스 링 육안 확인
- ⚠️ **shadcn CLI 는 쓰지 않는다.** 맥에서 실제로 돌려보고 내린 결론이다.
  - 지금 CLI 는 프리미티브를 고르게 하는데 기본값이 **Base UI** 다. 그걸 받으면
    Radix(0.6 의 `<Modal>`)와 프리미티브가 두 개가 된다. SHELL.md §3 과도 어긋난다.
  - `Radix UI` + `Custom` 을 골라도 CLI 가 설치하지 않고 웹 프리셋 빌더로 넘긴다.
    거기서 나오는 명령은 프리셋 색·폰트를 들고 와서 `globals.css` 토큰을 덮어쓴다.
  - 그래서 Radix 프리미티브만 npm 으로 받고 컴포넌트는 우리 토큰으로 직접 썼다.
    `src/components/ui/` 에 button / input / badge / skeleton / switch /
    dropdown-menu / alert-dialog. 프리미티브는 Radix 하나로 유지된다.
  - `toast` 는 뺐다. 최신 shadcn 에서 `sonner`(새 의존성)로 대체됐고 아직 쓰는 데가 없다.
    필요해지는 태스크에서 판단한다.

### [~] 0.6 셸 + 모달 인프라
> `SHELL.md` 전체가 이 태스크의 스펙이다. 여기가 어긋나면 P3~P5를 전부 다시 짠다.

- `layout.tsx` — 헤더 + `@modal` 슬롯
- `<Modal>` 래퍼 — 모바일 Sheet / 데스크톱 Dialog 분기, `router.back()` 닫기
- `<PageShell>` 래퍼 — 풀페이지 fallback용 (뒤로가기 헤더 포함)
- `@modal/default.tsx`
- 더미 모달 1개로 전 경로 검증
- **DoD**
  - 메인 → 모달 열기 → 뒤로가기 → 모달 닫히고 **지도 뷰 유지**(줌·중심 그대로)
  - 모달 URL 새로고침 → 풀페이지로 정상 렌더
  - 모달 URL 복사 → 새 탭 붙여넣기 → 정상 진입
  - iOS Safari에서 주소창 노출/숨김 시 모달 하단이 잘리지 않음
  - Esc / 배경 탭 / 아래로 드래그 세 경로 모두 닫힘
- **shadcn 대신 `@radix-ui/react-dialog` 위에 직접 만들었다.** `ui.shadcn.com` 이 차단돼
  CLI 를 못 쓰는데, shadcn 의 Dialog·Sheet 도 결국 이 패키지를 감싼 것이라 토대는 같다.
  SHELL.md §3 이 요구하는 건 "Radix 가 처리한다" 이고 그건 충족한다.
  나중에 `shadcn add dialog sheet` 를 돌려도 `<Modal>` 래퍼는 우리 것이므로 버릴 게 없다.
- ⚠️ **iOS Safari 주소창 확인만 남았다.** `dvh` + `env(safe-area-inset-bottom)` 으로
  짜뒀지만 실기기에서만 검증된다. 나머지 DoD 4개는 Chromium 360px/1280px 에서 확인.

---

## P1 — 지도 (핵심 리스크 구간, 먼저 뚫는다)

### [x] 1.1 네이버 지도 최소 렌더 ⚠️ 최우선
> 이 태스크가 막히면 프로젝트 전체가 막힌다. 가장 먼저 검증한다.
> **P0.2보다 먼저 시도해도 좋다.**

- NCP 콘솔에서 Web Dynamic Map 앱 등록, **서비스 URL에 `http://localhost:3000` 추가**
- `next/script` + `ncpKeyId` 파라미터로 SDK 로드
- `<NaverMap />` 클라이언트 컴포넌트: 고정 좌표 지도 1개 표시
- `window.navermap_authFailure` 핸들러
- 타입 선언 (`@types/navermaps` 또는 자체 `.d.ts`)
- **DoD**: 로컬에서 지도 표시 + 콘솔 에러 0건. 스크린샷 확인.
- ✅ **Vercel 배포본(`hannuri-matzip.vercel.app`)에서 지도 렌더 확인.** iPad Safari,
  폴백 좌표(시청역) 정상. 인증 통과 = NCP 서비스 URL 등록까지 맞다는 뜻.
  - NCP 는 `AI·NAVER API` 가 아니라 **독립 상품 `Maps`** 로 옮겨져 있다.
    콘솔 검색창에 `Maps` → Application 등록 → API 선택에서 **Dynamic Map**.
  - Web 서비스 URL 은 입력 후 **`+ 추가` 를 눌러야** 저장된다.
  - `NEXT_PUBLIC_*` 은 빌드 시 코드에 박히므로 Vercel 환경변수 추가 후 **재배포 필수**.
  - 로컬에서 돌리려면 NCP 서비스 URL 에 `http://localhost:3000` 을 추가로 등록한다.
  - 작업 환경에서는 `oapi.map.naver.com` 이 차단돼 에러 경로만 검증했었다
    (키 없음 / 로드 실패). 콘솔 에러 0건은 실기기에서 미확인.

### [x] 1.2 Geolocation 훅
- `useCurrentPosition()` — `SPEC.md §5` 상태 머신 구현
- denied/timeout 시 폴백 좌표 + 배너
- "내 주변 찾기" 버튼 트리거 경로
- **DoD**: 브라우저 위치 권한 허용/거부 두 시나리오 모두 정상 동작
- ⚠️ Chromium 으로 허용·거부·저정확도·캐시 4가지를 검증했다.
  `timeout` 분기와 지도 `panTo` 는 미검증 — 실기기에서 확인 필요.

### [x] 1.3 반경 Circle + 현재위치 마커
- `naver.maps.Circle` 반경 표시, 반경 변경 시 반지름 갱신
- 현재 위치 마커 (파란 점 + accuracy 원)
- 반경 변경 시 `map.fitBounds(circle.getBounds())`
- **DoD**: 30m/50m/100m/200m 토글 시 원과 줌 레벨이 함께 변함
- ✅ 실기기(Chrome)에서 파란 점·accuracy 원·반경 원·반경 토글 시 줌 변화 모두 확인.
- ⚠️ 반경 옵션을 **30 / 50 / 100 / 200m** 로 바꿨다 (원래 500m/1km/1.5km).
  기본값은 처음 50m 였다가 **200m** 로 다시 바꿨다 — 좁게 시작하면 처음 들어온
  사람이 "등록된 곳이 없어요" 만 보고 나간다.
  100m 넘게 걸어가서 먹는 일이 없다는 판단. SPEC §0 전제와 §3.1/§3.2 의
  `p_radius_m` 기본값도 200m 로 함께 맞췄다.

### [x] 1.4 지도 뷰 상태 스토어
- zustand — `center / zoom / radius / selectedId`만 보관 (서버 데이터 금지)
- 메인 마운트 시 복원, 지도 이동 시 저장 (debounce)
- **DoD**: 풀페이지 fallback 진입 → 뒤로가기 → 지도가 이전 위치·줌으로 복원됨
- ⚠️ 지도 SDK 가 차단된 환경이라 최소 스텁으로 갈아끼워 배선을 검증했다.
  실제 지도에서의 드래그·줌 저장은 실기기 확인 필요.
- 저장은 `sessionStorage`(탭 단위). 모듈 스토어만으로는 풀페이지 fallback 을
  새로고침으로 진입했다 돌아올 때 살아남지 못한다.
- 🐛 **복원 전에 지도를 만들면 카메라가 튄다.** `skipHydration` 이라 첫 렌더는 항상
  기본값이고, 그 상태로 지도를 만들면 복원 직후 저장값으로 바뀌며
  `fitBounds` 가 한 번 더 돈다. 조회도 50m 로 한 번, 200m 로 또 한 번 나갔다.
  `useMapViewHydrated()` 를 만들어 **복원이 끝난 뒤에** 지도를 만들고 조회를 시작한다.
  반경 토글도 복원 전에는 아무것도 눌린 걸로 보이지 않는다 — 잘못된 값이 잠깐
  눌려 있다가 바뀌면 사용자가 자기가 안 누른 변화를 보게 된다.
  검증: 저장된 200m 로 새로고침 시 관측 상태가 `none → 200m`(50m 없음), 지도 생성
  1회, 반경 원 200만, `fitBounds` 1회, RPC 반경 200만. 반례로 게이트를 빼면
  RPC 가 `50, 200` 으로 두 번 나간다.
- SSR 에서 `useMapView.persist` 는 **undefined 다.** sessionStorage 가 없으면 zustand 가
  persist API 자체를 안 붙인다. 프리렌더가 죽으므로 접근은 이펙트 안에서만 한다.
- **저장된 뷰가 있으면 위치를 얻어도 카메라를 옮기지 않는다.** 안 그러면 돌아올 때마다
  내 위치로 튕겨서 복원이 무의미해진다. 옮기는 건 사용자가 [내 주변 찾기]를 눌렀을 때뿐.

---

## P2 — 맛집 조회

### [x] 2.1 `restaurants_within` RPC
- `SPEC.md §3.1` 함수 작성
- GiST 인덱스 확인 (`explain analyze`로 Index Scan 확인 — Seq Scan이면 실패)
- **DoD**: 시드 데이터 기준 반경 200m 조회 결과가 육안으로 맞고, 실행 계획에 인덱스 사용됨
- ✅ `Index Scan using restaurants_location_idx` 확인.
  `Index Cond: location && _st_expand(..., 200)` — st_dwithin 이 인덱스로 내려간다.
- ⚠️ **`restaurant_stats` 뷰가 매 호출마다 전체 맛집을 집계한다.** 실행 계획에서
  반경 안 4건과 별개로 전체 행이 HashAggregate 를 탄다. 지금은 문제 없다 —
  500건 1.2ms / 2,000건 1.1ms / 10,000건 5.2ms 로 SPEC §6 예산(500건 100ms) 안이다.
  SPEC §2.2 도 "v1 은 뷰로 충분하다" 고 적어뒀다. 느려지면 §2.2 의 메모대로
  `restaurants` 에 집계 컬럼 + 트리거로 전환한다.

### [~] 2.2 시드 데이터
- 사무실 주변 실제 맛집 15곳 + 메뉴 + 더미 리뷰 스크립트
- **DoD**: `pnpm seed` 로 재실행 가능(멱등)
- `supabase/seed.sql` + `scripts/seed.sh` (`pnpm seed`). 맛집 15 / 메뉴 33 / 리뷰 24.
  id 를 이름의 md5 로 만들어서 멱등하다 — 4회 연속 실행에 건수·좌표 변화 0.
  반례로 id 를 `gen_random_uuid()` 로 바꿔 돌려보면 `restaurants_name_loc_uniq` 에
  걸려 트랜잭션이 통째로 롤백된다. 결정적 id 가 실제로 멱등성을 지탱하고 있다.
- 사용자가 앱에서 만든 행은 건드리지 않는다 (시드 실행 후에도 그대로인 것 확인).
- 좌표는 사무실 기준 (방위각°, 거리 m) 로 적는다. 30/50/100/200m 에서 각각
  2/4/8/13건이 잡히고, 205m·235m 두 곳은 최대 반경 밖에 둬서 토글이 실제로 달라진다.
  리뷰 0건인 곳 1개(베이글로드)를 남겨 뒀다 — 빈 상태 화면(6.1) 확인용.
- 기준점을 사용자가 준 좌표(37.537247, 127.082473)로 바꿨다. `.env.example` /
  `SPEC.md §1.2` / `config.ts` 의 폴백 기본값도 같이 맞췄다 — 네 곳이 갈라지면
  지도의 반경 원과 조회 결과가 다른 곳을 가리킨다.
  새 기준점에서 반경 30/50/100/200m 에 2/4/8/13건, 옛 기준점(서울시청)에서 0건.
- ⚠️ **가게 이름은 아직 합성이다.** 실제 15곳을 받으면 `seed_restaurants` 만
  갈아끼우면 된다 (파일 맨 아래에 교체 절차 적어둠). 그때 `[x]` 로 바꾼다.
- ⚠️ **기준 좌표가 대화에서 나온 주소와 다르다.** 사용자는 사무실 주소를
  "서울특별시 종로구 내자동 219" 라고 했는데, 준 좌표는 경도 127.08 로 내자동
  (126.97 대)에서 약 10km 떨어져 있다. 확인을 요청했지만 "그냥 해줘" 로 진행했다.
  사무실이 실제로 내자동이면 `seed_office` 와 `.env` 두 곳만 고치고
  `pnpm seed` 를 다시 돌리면 전부 따라온다.

### [x] 2.3 맛집 마커 렌더
- RPC 결과 → 마커 생성, `icon.content` 커스텀 HTML (평점 뱃지)
- **마커 diff 갱신** — 기존 마커 전체 삭제 후 재생성 금지
- 마커 클릭 → 선택 상태
- **DoD**: 필터 변경 시 깜빡임 없음, 마커 수가 리스트 항목 수와 일치
- TanStack Query 도입(고정 스택). `src/app/providers.tsx` 가 QueryClientProvider 와
  `SessionBootstrap` 을 함께 들고 있다. `src/features/restaurants/` 에
  `api.ts`(RPC + 줄 단위 좁히기) / `use-nearby.ts`(쿼리 훅).
- **검색 기준은 지도 중심이 아니라 `anchor` 다** — 내 위치, 없으면 사무실 폴백.
  지도를 끌어도 반경 원과 조회 결과가 같은 곳을 가리킨다. 그래서 반경 원 중심도
  `anchor` 를 따라가게 바꿨다 (1.3 때는 지도 중심이었다).
- 깜빡임은 두 장치가 함께 막는다. 하나만 빠져도 깨진다:
  - `keepPreviousData` — 없으면 전환 중 빈 배열을 거쳐 마커가 0개가 된다
  - 마커 diff — 없으면 4개를 남기고 9개만 만들 것을 13개 전부 새로 만든다
- 마커 배지는 `<button>` 이라 키보드로 닿고 Enter 로 선택된다. `aria-label` 에
  이름·별점·리뷰 수를 넣었다. 리뷰 0건은 `0.0` 이 아니라 `–` 다 — 0.0 은 "0점" 으로
  읽히는데 그건 다른 뜻이다.
- 맛집 이름은 사용자 입력이라 `icon.content` 에 넣기 전에 이스케이프한다.
- 검증(Playwright, 네이버 SDK + Supabase 스텁) 13개 항목 전부 통과, 페이지 에러 0건.
  반경 30/50/100/200m 에서 마커 2/4/8/13개, 전환 중 0개가 되는 순간 없음,
  200m 전환 시 새로 만든 마커 9개, 선택 변경은 마커를 재생성하지 않음,
  조회 실패 시 "맛집을 불러오지 못했어요" + [다시 시도], 360px 가로 넘침 0px.
  반례 2종 확인 — `keepPreviousData` 를 빼면 깜빡임이 관측되고, diff 를 빼면
  생성 수가 9 → 13 으로 뛴다.
- DoD 후반("마커 수 = 리스트 항목 수")은 2.4 에서 확인했다. 지도와 리스트가
  `MainView` 의 **같은 배열**을 보므로 구조적으로 어긋날 수 없다 —
  50m 4/4, 200m 13/13, 일식 필터 3/3.

### [x] 2.4 리스트 + 필터바
- 맛집 카드 컴포넌트
- 반경 토글, 정렬(거리/별점/리뷰수), 카테고리 칩 다중 선택
- 모바일 드래그 시트 / 데스크톱 2단 분할
- **DoD**: 360px 폭에서 레이아웃 깨짐 없음
- `MainView` 가 조회·필터·정렬을 한 곳에서 들고 지도와 리스트에 **같은 배열**을
  내려준다. `MapPanel` 은 그만큼 얇아졌다 — 쿼리와 반경 토글이 빠졌다.
- 반경 토글을 지도 위 오버레이에서 필터바로 옮겼다. SPEC §4.1 레이아웃이 그 자리에
  두고 있고, 지도 위에 겹쳐 두면 마커를 가린다.
- **카테고리는 서버가, 정렬은 클라이언트가 한다.** RPC 가 `p_categories` 를 이미
  받으므로 거르는 건 서버가 싸고, 정렬까지 서버에 다녀오면 정렬을 바꿀 때마다
  리스트가 비었다 채워진다.
- 카테고리·정렬은 zustand 에 넣지 않았다 — 스토어는 지도 뷰 상태만 담는다
  (CLAUDE.md). 대신 `MainView` 가 들고 있어서 모달을 열고 닫아도 유지된다.
  풀페이지 fallback 을 다녀오면 초기화되는데, 반경만큼 되돌릴 필요가 큰 값은 아니라고 봤다.
- 정렬 동점은 항상 거리순으로 푼다. 안 그러면 필터를 바꿀 때마다 같은 별점끼리
  순서가 뒤바뀌어 리스트가 뒤섞인 것처럼 보인다. 리뷰 0건은 별점순에서 맨 뒤다 —
  `avg_rating` 이 0 이지만 "0점" 이 아니라 "아직 없음" 이다.
- 빈 상태를 두 갈래로 나눴다. 필터 때문이면 "이 조건에 맞는 곳이 없어요"
  + [카테고리 전체 보기], 정말 없으면 "이 반경에는 아직 등록된 곳이 없어요"
  + [맛집 등록하기]. 원인이 다르면 다음 행동도 다르다.
- 드래그 시트는 **드래그 전용이 아니다.** 손잡이가 `button` 이라 키보드로도 접힌다
  (`aria-expanded`). 실제로 끌었을 때만 뒤따르는 click 을 한 번 삼킨다 —
  반례로 그 한 줄을 빼면 드래그가 두 번 토글돼 제자리로 돌아온다.
- 시트를 접었다 펴면 지도 컨테이너 높이가 바뀌므로 `ResizeObserver` 로
  `map.refresh()` 를 알린다. 타입에는 있지만 SDK 버전에 따라 없을 수 있어
  존재 확인 후 부른다 — ResizeObserver 콜백에서 던지면 이후 리사이즈가 전부 죽는다.
- 검증(Playwright, 네이버 SDK + Supabase 스텁) 23개 항목 전부 통과, 페이지 에러 0건.
  360px 가로 넘침 0px / 문서 세로 스크롤 0px(리스트 안에서만 스크롤) /
  칩 한 줄 34px / 데스크톱 40:60 2단, 리스트가 왼쪽 / 손잡이는 `lg` 에서 숨음 /
  정렬 3종 · 카테고리 필터가 RPC 파라미터까지 전달 / 카드가 키보드 포커스를 받음.
  드래그는 별도로 위·아래·짧은 탭 3가지를 확인했다.
- ⚠️ 실기기 미확인. `map.refresh()` 후 실제 지도가 제대로 다시 그려지는지는
  스텁으로는 알 수 없다. iOS Safari 확인 때 같이 본다 (0.6).

### [x] 2.5 지도↔리스트 양방향 연동
- 카드 hover/focus → 마커 강조
- 마커 클릭 → 리스트 스크롤 + 하이라이트
- **DoD**: 양방향 모두 동작, 스크롤이 튀지 않음
- **`selectedId` 만으로는 못 만든다. 선택의 출처가 필요하다** (`select-source.ts`).
  같은 id 라도 어디서 골랐냐에 따라 반응이 달라야 하고, 안 그러면 지도와 리스트가
  서로를 밀어서 화면이 계속 튄다:
  - `map` → 리스트를 그 카드로 스크롤. 지도는 안 옮긴다(방금 누른 마커로 튄다)
  - `list` → 마커만 강조. 스크롤도 지도 이동도 안 한다(보던 자리가 튄다)
  - `null` → 앱 밖에서 온 것(점메추 [여기로 갈게요]). 이때만 지도를 옮긴다
  `MainView` 가 마지막으로 자기가 시킨 선택을 기억해서 출처를 판정한다. 스토어의
  `selectedId` 가 자기가 안 시킨 값으로 바뀌면 그게 곧 "밖에서 왔다" 는 뜻이다.
- 스크롤은 `block:"nearest"`. 가운데로 끌어오면 마커를 누를 때마다 리스트가 크게
  뛴다 — 이미 보이는 카드를 누르면 스크롤 값이 그대로인 것을 확인했다.
- 마커 강조는 색만 바꾸지 않는다. `scale-110` + 한 번짜리 `marker-pop`.
  반복 애니메이션은 안 쓴다 — 지도 위에서 계속 움직이면 나머지를 읽기 어렵다.
  `prefers-reduced-motion` 은 기존 전역 미디어쿼리가 0.01ms 로 줄인다.
- **함께 고친 레이아웃 버그.** `body` 가 `min-h-dvh` 라 리스트가 길어지면 자기
  안에서 스크롤하지 않고 문서 전체를 밀어냈다(지도가 위로 밀려 올라감).
  항목 4개일 때는 안 보이다가 13개에서 드러났다 — 2.4 검증을 통과했던 이유다.
  `h-dvh` 로 바꿔서 메인이 헤더를 뺀 높이에 고정되고 리스트가 그 안에서 스크롤한다.
  풀페이지 fallback 은 `PageShell` 이 `min-h-dvh` 를 들고 있어 그대로 문서가
  스크롤된다 — 상세 3882px / `/design` 4083px 스크롤 확인.
- 검증 10개 항목 전부 통과, 페이지 에러 0건. 기존 3종(2.3 / 2.4 / P3·P4)도
  회귀 없이 통과.

---

## P3 — 리뷰

### [x] 3.1 상세 화면 (모달 + fallback)
- `RestaurantDetail` 컴포넌트 1개를 `@modal/(.)restaurants/[id]`와
  `restaurants/[id]` 양쪽에서 사용 — **복붙 금지**
- 정보, 평점 분포, 메뉴, 리뷰 목록
- `dynamic()` 지연 로드
- **DoD**: 리뷰 0건 상태의 빈 화면도 자연스럽고, 모달·풀페이지 두 경로 모두 동일하게 보임
- `restaurant-detail.tsx`(dynamic 래퍼) → `restaurant-detail-view.tsx`(본문).
  두 라우트가 래퍼 하나를 import 한다. 리뷰 폼·이미지 리사이즈 코드가 메인 번들에
  실리지 않는다.
- 조회는 한 번에 한다 — `restaurants` 에 `menus`, `reviews(profiles, review_photos)`
  를 임베드. 세 번 왕복하면 로딩이 세 단계로 쪼개져 보인다.
- 평균 별점은 뷰(`restaurant_stats`)와 같은 규칙으로 반올림한다. 갈리면 리스트와
  상세가 다른 숫자를 보여준다.
- 분포 막대는 막대만 두지 않고 개수를 숫자로 함께 적는다.

### [x] 3.2 리뷰 작성/수정
- **상세 모달 내부 뷰 전환.** 별도 모달을 띄우지 않는다 (모달 2단 금지)
- 별점 선택 UI (키보드 조작 가능), 텍스트, 방문일
- upsert (unique 제약 활용), 본인 리뷰 수정/삭제 (삭제는 `AlertDialog`)
- **DoD**: 같은 유저가 두 번 작성 시도 → 기존 리뷰 수정으로 동작
- `RatingInput` 은 `radiogroup`. **화살표로 값을 바꾸면 포커스도 함께 옮긴다** —
  처음엔 값만 바꿨는데, 그러면 스크린리더가 계속 원래 별을 읽어서 지금 몇 점인지
  소리로는 알 수 없다. 검증에서 잡아 고쳤다.
- textarea 에 텍스트 크기 클래스를 두지 않는다. globals 의 16px 이 살아야
  iOS 가 자동 확대하지 않는다.

### [x] 3.3 사진 업로드
- 클라이언트 리사이즈(장변 1600px) + WebP 변환 → Storage 업로드
- 최대 3장, 업로드 진행 표시
- **DoD**: 5MB 원본 업로드 시 저장 용량 500KB 이하
- **1600px/q0.8 한 번으로는 DoD 를 못 지킨다.** 노이즈가 많은 사진은 그 설정에서
  678KB 가 나온다. 그래서 500KB 예산을 두고 (1600,0.8) → (1600,0.65) →
  (1280,0.65) → (1280,0.5) → (1024,0.5) 로 단계적으로 낮춘다.
  측정: 일반 사진은 1단계 31KB, 최악(노이즈 24MB)은 3단계 397KB.
- 경로 첫 조각이 반드시 uid 다. Storage RLS 가 `foldername(name)[1] = auth.uid()`
  로 소유자를 판별한다 (SPEC §2.3).
- 사진은 "교체" 다. 새로 고르면 예전 파일을 Storage 에서 지우고 다시 올린다.
  안 고르면 기존 사진이 남는다는 걸 폼에 문장으로 알린다.
- 리뷰 삭제 시 Storage 파일도 지운다 — DB cascade 는 Storage 를 따라오지 않는다.

### [x] 3.4 평점 집계 반영
- 리뷰 작성 후 리스트/마커 평점 갱신 (TanStack Query invalidate)
- **DoD**: 리뷰 남기고 모달 닫으면 배경 리스트의 평균 별점이 즉시 반영됨
- 저장·삭제 뒤 `["restaurant", id]` 와 `["restaurants","nearby"]` 를 함께 무효화한다.
  뒤쪽을 빼먹으면 상세만 바뀌고 배경 리스트·마커는 옛 별점을 들고 있는다.

---

## P4 — 점메추 / 저메추

### [x] 4.1 `pick_restaurant` RPC
- `SPEC.md §3.2` 로직 — 필터, 최근 제외, 가중 랜덤, 후보 0건 폴백
- **DoD**: 동일 파라미터로 20회 호출 시 결과가 분산되고, 평점 높은 곳이 더 자주 나옴
- ✅ 300회 측정: 고평점(5.0) 73% / 저평점(1.0) 18% / 신규(리뷰 0건) 9%.
  weight = (avg_rating+1)^1.5 의 기대값(79/15/5)과 맞는다. 신규도 뽑힌다.
- SPEC 에 없는 `p_exclude_ids` 를 추가했다. 4.4 의 DoD("연속 5회 재추첨에 같은
  곳이 다시 나오지 않는다")를 서버에서 지키려면 필요하다. 기본값이 있어 기존
  호출은 그대로 동작한다.
- 후보 0건 폴백은 **"최근 간 곳" 조건만** 푼다. 반경·카테고리·가격대는 사용자가
  고른 조건이라 넓히지 않고, `p_exclude_ids` 도 유지한다 — 그걸 풀면
  [다시 뽑기] 가 같은 곳을 다시 준다.
- 임시 테이블 대신 CTE 로 짰다. `base` 를 두 번 참조해도 `restaurants_within` 은
  한 번만 돈다.
- ⚠️ 측정할 때 `select (select ... from pick_restaurant(...)) from generate_series(1,200)`
  로 재면 200회가 전부 같은 값이다. 상관 없는 서브쿼리라 한 번만 평가된다 —
  함수가 결정론적인 게 아니다. 루프로 재야 한다.

### [x] 4.2 `/pick` 모달
- 시간대 기반 점심/저녁 기본값
- 필터 UI (카테고리, 가격대, 반경, 최근 제외 스위치)
- **DoD**: 필터 조합이 RPC 파라미터로 정확히 전달됨
- 11~15시는 점심, 나머지는 저녁. 서버 렌더와 어긋나지 않게 마운트 후에 정한다.
- 검증에서 `p_categories:["일식"] / p_max_price:2 / p_exclude_days:7` 이 그대로
  전달되는 것을 확인했다.

### [x] 4.3 추첨 애니메이션
- **서버 응답 수신 후** 셔플 애니메이션 1.2s → 결과 카드
- `prefers-reduced-motion` 시 즉시 표시
- **DoD**: 네트워크 느릴 때도 애니메이션 도중 결과가 바뀌지 않음
- 셔플은 `submittedAt` 이 바뀔 때 시작한다. 시작 시점에 결과는 이미 손에 있으므로
  애니메이션 도중 값이 바뀔 수가 없다 — 이게 DoD 를 구조적으로 만족시키는 방식이다.
- 검증: 응답 후 1.7초 뒤 결과 표시, `reducedMotion: reduce` 컨텍스트에서는 0.16초.

### [x] 4.4 수락 / 재추첨 로그
- [여기로 갈게요] / [다시 뽑기] → `recommendation_logs` 기록
- [여기로] 선택 시 모달 닫고 배경 지도가 해당 맛집으로 이동 + 마커 선택
- 세션 내 재추첨 시 직전 결과 제외
- 후보 0건 안내 + [반경 넓히기]
- **DoD**: 연속 5회 재추첨 시 같은 곳이 다시 나오지 않음
- 뽑는 즉시 `accepted = null` 로 로그를 남기고, [여기로]=true / [다시]=false 로
  그 행을 update 한다. 뽑아만 놓고 안 간 경우도 남아야 통계가 의미를 가진다.
- 직전 결과 제외는 클라이언트가 모은 `seen` 을 서버 `p_exclude_ids` 로 넘겨서 한다.
  클라이언트에서 걸러내면 후보가 하나 남았을 때 빈 결과와 구분이 안 된다.
- ✅ 연속 5회 재추첨에 중복 0, 제외 목록이 매회 커지고, 후보가 떨어지면
  안내 + [반경 넓히기] 가 뜬다.
- 지도 이동은 **밖에서 선택했을 때만** 한다. 마커를 눌러 고른 경우엔 안 옮긴다 —
  방금 누른 마커로 지도가 튀면 성가시다. `MapPanel` 이 출처를 구분한다.

- **P3·P4 공통 검증** (Playwright, 네이버 SDK + Supabase 스텁) 27개 항목 전부 통과,
  페이지 에러 0건. 사진 예산은 별도로 측정했다.
- ⚠️ 실물 Supabase 미검증. Storage 업로드·서명 URL·`pick_restaurant` 실호출은
  스텁으로만 확인했다. `supabase db push` 로 마이그레이션을 올린 뒤 실기기에서
  리뷰 사진 1장과 점메추 1회를 직접 해봐야 한다.

---

## P5 — 등록 / 마이

### [x] 5.1 네이버 지역검색 프록시
- `POST /api/places/search` — 서버에서만 secret 사용
- 응답 파싱: `lng = mapx/1e7`, `lat = mapy/1e7` — **실제 응답으로 1회 검증 후 확정**
- HTML 태그 제거(`<b>` 포함되어 옴), 최대 5건 제약 UI에 반영
- **DoD**: 실제 상호 검색 시 좌표가 지도상 올바른 위치에 찍힘
- ⚠️ **API 가 이사했다. SPEC §4.4 를 실제에 맞게 고쳤다.**
  네이버 개발자센터 앱 등록 폼의 `사용 API` 목록에 `검색` 이 더 이상 없다.
  지금은 **NAVER API HUB**(`console.ncloud.com/naver-api-hub`, NCP 중개 운영)에서
  `지역` 을 골라 발급받는다. 호출 정보가 전부 바뀐다:
  | | 구 | 현재 |
  |---|---|---|
  | 도메인 | `openapi.naver.com` | `naverapihub.apigw.ntruss.com` |
  | 경로 | `/v1/search/local.json` | `/search/v1/local` |
  | 헤더 | `X-Naver-Client-Id` | `X-NCP-APIGW-API-KEY-ID` |
  | 헤더 | `X-Naver-Client-Secret` | `X-NCP-APIGW-API-KEY` |
  콘솔은 발급값을 여전히 "Client ID/Secret" 이라고 부르지만 헤더 이름은 NCP 규약이다.
  `.env` 변수명(`NAVER_SEARCH_*`)은 그대로 두고 헤더에서만 바꿨다.
- **좌표는 스케일을 추측하지 않고 판정한다.** SPEC 은 10^7 배라고 적어뒀지만 과거
  KATECH 이력이 있어서, 10^7 → 10^6 → 원본 순으로 나눠 보고 한국 범위
  (경도 124~132, 위도 33~39)에 들어오는 것을 쓴다. 어느 것도 안 맞으면
  **그 후보를 버린다** — 틀린 좌표로 등록되면 지도에 엉뚱한 곳이 박히고 되돌리기 어렵다.
- 검색 실패가 등록 실패는 아니다. 키가 없으면 503 + "이름과 위치를 직접 입력해
  주세요", 429 면 "조금 뒤에 다시", 그 외는 502 + "직접 입력해도 등록할 수 있어요".
  SPEC §4.4 가 요구하는 수동 입력 경로를 항상 열어 둔다.
- 검증 14개 항목 전부 통과 (가짜 업스트림을 세우고 프록시를 실제로 호출).
  `<b>` 제거 / 좌표 변환 / KATECH 후보 제외 / NCP 헤더 전송 / 구 헤더 미전송 /
  경로·display=5 / 짧은·긴 검색어 400 / 429·500 처리. 좌표 단위 테스트 9종 별도.
- **시크릿 유출 없음 확인.** 카나리 값으로 빌드해 `.next/static` 을 뒤져 0건.
  같은 방법으로 공개 변수(`NEXT_PUBLIC_SUPABASE_URL`)는 3개 청크에서 발견 —
  검사 방법 자체가 유효하다는 뜻이다.
- ✅ **실물 호출로 엔드포인트와 좌표 스케일을 확정했다** (맥북, 2026-08-08).
  `/search/v1/local` 이 맞고 NCP 헤더로 200 이 온다. `경복궁` 응답의
  `mapx "1269770162"` / `mapy "375788408"` → 126.9770162 / 37.5788408 로 **10^7 이 맞다.**
  이 응답을 검증 픽스처로 박아 뒀다 — 나중에 스케일이 또 바뀌면 여기서 깨진다.
- 첫 시도는 `Authentication information are missing` 이었는데 키가 틀린 게 아니라
  셸에 환경변수가 안 올라간 것이었다. `.env.local` 은 셸이 자동으로 읽지 않는다:
  `set -a; source .env.local; set +a`.
- 라우트가 응답을 캐시하므로(revalidate 300) 검증도 매번 다른 검색어를 써야
  업스트림까지 실제로 나간다. 안 그러면 헤더·경로 확인이 캐시에 가려 통과해 버린다.

### [x] 5.2 맛집 등록 모달
- `SHELL.md §4`의 3단계 방식: 검색 → **모달 축소 + 배경 지도로 핀 조정** → 부가정보
- **모달 안에서 `new naver.maps.Map()` 호출 금지**
- 검색 실패 시 수동 입력 경로
- 중복 등록 방지 (unique 위반 시 기존 맛집으로 안내)
- **DoD**: 지역검색 결과 없는 가게도 수동으로 등록 가능,
  핀 조정 중 지도 인스턴스가 1개인지 확인
- ✅ 핀 조정 중 지도 컨테이너 1개, 모달 188px(하단), 지도를 움직이면 좌표가 폼에
  따라온다. 등록된 좌표는 검색 좌표가 아니라 **핀으로 잡은 값**이다.
- 등록 모달과 배경 지도는 다른 라우트 트리(`@modal` vs `children`)라 props 를 못
  넘긴다. `pin-store.ts` 가 둘 사이의 손잡이다. **좌표는 거기 두지 않았다** —
  지도가 이미 `map-store.center` 에 쓰고 있어서 폼은 그걸 읽는다. 같은 값을 두 군데
  두면 반드시 갈라진다.
- `<Modal variant="dock">` 추가. 하단 220px, 오버레이 투명 + 클릭 통과,
  아래로 끌어 닫기 비활성(지도 만지다 폼이 사라지면 안 된다).
- 🐛 **오버레이를 조건부로 언마운트했다가 모달 전체가 안 눌리는 버그가 났다.**
  Radix Portal 은 자식을 마운트 순서대로 붙여서, 껐다 켠 오버레이가 Content
  *뒤에* 다시 붙는다. 그러면 z-index 가 같아 오버레이가 위를 덮는다.
  언마운트하지 않고 스타일만 바꾸는 걸로 고쳤다. 검증에서 잡혔다.
- 🐛 **360px 에서 메뉴 행이 68px 넘쳤다.** `Input` 의 `w-full` 때문에 flex 에서
  이름 칸이 안 줄어든다. `grid-cols-[minmax(0,1fr)_5rem_auto]` 로 바꿔 해결.
- 풀페이지 fallback 에는 배경 지도가 없다. 지도를 새로 만들지 않기로 했으므로
  (`canPin={false}`) 핀 단계를 건너뛰고 검색 좌표를 쓰며, 그 사실을 화면에 적는다.
- 대표메뉴는 하나만 선택된다 — 여러 개면 상세에서 뱃지가 줄줄이 붙는다.
- 메뉴 저장이 실패해도 맛집 등록은 성공으로 처리한다. 여기서 던지면 사용자가
  다시 등록하려다 중복 에러를 만난다. 메뉴는 5.3 에서 다시 넣을 수 있다.
- 검증 24개 항목 전부 통과, 페이지 에러 0건. 기존 4종(2.4 / 2.5 / P3·P4 / 5.1)도
  회귀 없이 통과.

### [x] 5.3 메뉴 관리
- 상세 화면에서 메뉴 추가/수정/삭제, 대표메뉴 지정
- **DoD**: 대표메뉴가 추천 결과 카드에 노출됨
- `menu-api.ts` + `menu-editor.tsx`. 상세 안에서 **섹션만 편집 모드로 바뀐다** —
  상세가 이미 모달이라 별도 모달을 띄우면 2단이 된다 (SHELL.md §3).
- **저장은 지우고 다시 넣지 않고 바뀐 것만 손댄다.** 전부 지웠다 넣으면 id 가 매번
  바뀌어서, 나중에 메뉴에 사진이나 좋아요가 붙으면 그게 통째로 끊긴다.
  검증에서 수정 1 / 삭제 1 / 추가 1 로 정확히 나가는 것을 확인했고, 반례로
  전부 재삽입하게 바꾸면 6개 항목이 깨진다.
- 대표메뉴는 하나만 — 여러 개면 결과 카드에 뭘 쓸지 정해지지 않는다.
  새 대표를 고르면 기존 대표가 같은 저장에서 해제된다.
- `menus` 는 등록자로 묶여 있지 않다 (SPEC §2.3). 누구나 고칠 수 있다는 걸
  편집 화면에 한 줄로 적어 뒀다 — 모르고 남의 걸 고쳤다고 생각하지 않게.
- 메뉴가 0건이면 "아직 메뉴가 없어요. 가보셨다면 하나 채워 주세요" + [메뉴 추가].
  예전에는 메뉴 섹션 자체가 사라져서 채울 방법이 없었다.
- **DoD(대표메뉴가 결과 카드에)**: `pick_restaurant` 는 `restaurants_within` 과 같은
  컬럼이라 메뉴를 안 준다. 클라이언트가 추천 로그 기록과 **병렬로** 한 번 더
  가져온다 — 어차피 셔플 1.2초 뒤에 뜨므로 지연이 보이지 않는다.
  대표메뉴가 없으면 그 줄만 안 그린다 (카드는 메뉴 없이도 멀쩡해야 한다).
- 검증 15개 항목 전부 통과, 페이지 에러 0건, 360px 넘침 0px.

### [x] 5.4 `/me` 모달
- 내 리뷰 / 내가 등록한 맛집 / 추천 히스토리
- 닉네임 변경
- **세션 소실 고지** — "이 기기에서만 내 기록으로 인식됩니다" 한 줄
- **DoD**: 각 섹션 빈 상태 UI 존재
- 네 쿼리를 `Promise.all` 로 한 번에 띄운다. 순차로 하면 로딩이 세 번 쪼개져 보인다.
- 빈 상태마다 갈 곳을 하나씩 준다 (둘러보기 / 등록하기 / 점메추 열기).
- 추천 기록의 세 상태(갔음·다시 뽑음·안 고름)를 **글자로** 쓴다. 색만으로 구분하지 않는다.
- 맛집이 지워지면 임베드가 비어 온다 → "지워진 맛집" 으로 줄은 남긴다.
- 이름을 바꾸면 `["restaurant"]` 도 무효화한다. 상세의 리뷰 작성자 이름이 같은 값을 쓴다.
- 검증 7개 항목 통과 (빈 상태 3종, 갈 곳 3종, 닉네임 저장·복귀, 추천 3상태).

---

## P6 — 마감

### [x] 6.1 로딩 / 에러 / 빈 상태
- 스켈레톤, 에러 바운더리, 각 화면 빈 상태 문구
- **DoD**: 네트워크 차단 상태에서도 앱이 흰 화면으로 죽지 않음
- 스켈레톤과 화면별 빈 상태는 P2~P5에서 각 화면과 같이 넣었다. 여기서 더한 건
  **바운더리 세 장**이다: `error.tsx`(라우트) / `global-error.tsx`(layout 붕괴) /
  `not-found.tsx`(없는 주소).
- `global-error.tsx` 는 전역 CSS 도 못 믿는 자리라 인라인 스타일로 쓴다.
  토큰 규칙의 유일한 예외이고, 이유를 파일에 적어 뒀다.
- 흰 화면은 두 경로로 온다. 둘 다 막았는지 따로 확인했다:
  ① API 전멸 → 쿼리 에러 상태 (셸·헤더는 살아있고 "맛집을 불러오지 못했어요")
  ② 렌더 중 예외 → `error.tsx` ("화면을 그리지 못했어요" + 다시 시도)
  ②는 MeView 를 일부러 던지게 만들어 확인했다.

### [x] 6.2 접근성
- 키보드 전용 조작으로 전 플로우 완주
- 모달 focus trap, Esc 닫기, 닫은 뒤 트리거로 포커스 복귀
- 별점 색+숫자 병기, 포커스 링, 명도 대비 4.5:1
- **DoD**: 마우스 없이 진입→검색→리뷰 작성 완료 가능
- 키보드 완주 실측: Tab 20번에 맛집 카드 → Enter 로 상세 모달 → 모달 안에서
  [리뷰 남기기] → 별점 라디오 → ArrowRight ×5 로 5점 → 본문 입력 → [리뷰 남기기]
  → 저장까지 마우스 0회. Esc 로 닫으면 포커스가 트리거(`A`)로 돌아온다.
- **별점 색을 바꿨다.** `--color-star` 가 흰 배경에서 2.15:1 이었다. 별은
  "이 숫자가 평점이다" 를 알리는 그래픽이라 1.4.11(3:1)을 넘겨야 한다.
  `#F0A020` → `#C87C08` (3.3:1). 별 SVG·마커 ★ 가 전부 이 토큰을 쓴다.
- 명도 대비는 눈으로 보지 않고 계산해서 검사한다. `main`/`header` 아래
  텍스트 노드 전부를 훑고, 큰 글씨는 3:1, `aria-hidden` 글리프도 그래픽
  기준 3:1 로 잰다 — 안 재면 흐린 별이 그냥 통과한다.
- 음성 테스트로 검사기가 살아있는지 확인했다: 별 색을 원래대로 되돌리면
  `"★" 2.15:1(필요 3)` 으로 잡힌다.

### [~] 6.3 Vercel 배포 — 절차는 문서화, DoD는 실기기 필요
- 환경변수 설정, **NCP 서비스 URL에 배포 도메인 추가** → `README.md` 「배포」 절
- **DoD**: 실제 사내 폰(iOS Safari + Android Chrome)에서 GPS 동작 확인 → **미완.**
  GPS 는 HTTPS 에서만 뜨고, 이 환경에는 폰이 없다. 사람이 해야 한다.
- 프로덕션 빌드(`pnpm build` + `next start`)로 전 검증을 다시 돌려서 통과시켰다.
  dev 서버에서만 확인하면 놓치는 게 있다 — 실제로 아래 함정을 여기서 잡았다.
- ⚠️ **`NEXT_PUBLIC_*` 은 빌드 때 번들에 박힌다.** Vercel 에 나중에 넣으면
  재배포 전까지 안 먹는다. 증상이 조용해서 나쁘다: 지도는 "지도 키가 없어요",
  `/me` 는 "아직 준비되지 않았어요" 로만 뜨고 에러는 안 난다.
- 배포 후 두 가지가 더 필요하다: NCP 서비스 URL 에 배포 도메인 추가(빠뜨리면
  지도만 401), Supabase 익명 로그인 허용(꺼져 있으면 세션이 422).

### [~] 6.4 시드 + 온보딩 — 안내는 완료, 실데이터·DoD는 사람 필요
- 사무실 주변 맛집 실데이터 입력 → **미완.** 2.2 의 가짜 이름 15곳이 그대로다.
  실제 상호는 물어봐야 한다.
- [x] 첫 사용자용 짧은 안내 (3스텝) — `/welcome`
- **DoD**: 동료 2명이 설명 없이 리뷰 1건씩 작성 성공 → **미완.** 사람이 해야 한다.

안내 3스텝:
- `useState` 오버레이가 아니라 라우트다 (`/welcome` + `@modal/(.)welcome` + fallback).
  다른 화면과 같은 규칙을 쓴다 (SHELL.md).
- 메인에서 `router.push` 로 연다. 그래야 닫기(`router.back()`)가 지도로 돌아온다.
  같은 URL 로의 push 는 라우터가 합치므로 이펙트가 두 번 돌아도 히스토리가
  두 번 쌓이지 않는다 — `reactStrictMode` 를 켜고 확인했다.
- "봤다" 는 `localStorage` (`hannuri-matzip.welcome`). 탭 닫을 때마다 다시 뜨면
  안내가 아니라 방해라서 `sessionStorage` 가 아니다.
- 뜬 순간 봤다고 친다. 건너뛰기도 마찬가지. 대신 `/me` 에 다시 보기 링크를 뒀다.
- 검증 20개 항목 통과 (자동 노출, 스텝 이동, 재방문 미노출, 건너뛰기, 다시 보기,
  풀페이지 fallback, 키보드, 360px).
- ⚠️ 기존 검증 스크립트는 전부 첫 방문이라 안내가 먼저 뜬다.
  컨텍스트마다 `localStorage` 플래그를 심어 두고 돌린다.

---

## 순서 원칙

1. **P1.1(지도 렌더)을 P0.2보다 먼저 시도해도 좋다.** 외부 의존성 리스크가 가장 크므로,
   막힌다면 하루라도 빨리 아는 편이 낫다.
2. P0.5 / P0.6은 P1 이전에 반드시 끝낸다. 나중에 하면 전 화면을 다시 손봐야 한다.
3. P2까지 끝나면 이미 "쓸모 있는 앱"이다. P4(점메추)는 재미 요소이므로 P3 이후.
4. 각 P 단계 종료 시점에 `pnpm build` + 실기기 확인 1회.

## 하지 말 것

**데이터 / 로직**
- 네이버 플레이스 리뷰/평점 크롤링
- `st_distance(...) < radius` 형태의 반경 쿼리 (인덱스 미사용)
- 클라이언트 랜덤으로 추천 결과 결정
- `SUPABASE_SERVICE_ROLE_KEY` / 네이버 검색 시크릿의 클라이언트 노출

**지도**
- 마커 전체 삭제 후 재생성
- 지도 인스턴스 2개 이상 생성 (모달 안에서 새 지도 금지)

**화면 구조**
- `useState`로 여는 모달 (URL 없는 모달)
- 모달 위 모달 (`AlertDialog` 제외)
- 인터셉트 라우트와 fallback에 같은 UI를 복붙
- `100vh` 사용 (→ `dvh`)

**스타일**
- 컴포넌트 파일에 hex / raw px 하드코딩
- Tailwind 기본 팔레트 직접 사용 (`bg-orange-500`, `text-gray-600` 등)
- `styletron` / `baseui` 도입
- 별점을 색으로만 표현
- `dark:` 클래스 작성 (v1 다크모드 제외)
