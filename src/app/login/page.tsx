// 유일한 진짜 풀페이지. 모달이 아니다 (SHELL.md §1). 0.4에서 매직링크로 채운다.
export default function LoginPage() {
  return (
    <main className="min-h-dvh p-6">
      <h1 className="text-2xl font-bold">로그인</h1>
      <p>사내 메일로 로그인 링크를 받는 자리예요.</p>
    </main>
  );
}
