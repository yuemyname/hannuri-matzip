/**
 * 맛집 선택이 어디서 왔는지 (WBS 2.5).
 *
 * 같은 `selectedId` 라도 출처에 따라 화면이 달라져야 한다 —
 * 지도에서 고르면 리스트가 따라 스크롤하고, 리스트에서 고르면 지도는 가만히 있는다.
 * 출처를 안 들고 다니면 서로가 서로를 밀어서 화면이 계속 튄다.
 */
export type SelectSource = "map" | "list";
