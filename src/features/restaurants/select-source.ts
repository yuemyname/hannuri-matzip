/**
 * 맛집 선택이 어디서 왔는지 (WBS 2.5).
 *
 * 출처를 안 들고 다니면 지도가 자기가 만든 선택에 다시 반응해서 화면이 튄다.
 * 마커를 눌렀는데 지도가 그 마커로 이동하면, 방금 누른 것으로 화면이 움직인다.
 *
 * `null`(이 타입 밖)은 앱 밖에서 온 선택이다 — 점메추의 [여기로 갈게요].
 * 그때는 반대로 **지도를 옮겨야** 어디인지 보인다.
 *
 * 리스트를 없애면서 `"list"` 가 빠졌다. 값이 하나뿐이라도 타입은 남긴다 —
 * "출처가 map 이냐 아니냐"가 지도 이동 여부를 가르는 조건 그 자체다.
 */
export type SelectSource = "map";
